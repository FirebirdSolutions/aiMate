# aiMate Docker Compose Configuration
# Copy this file to .env and configure your environment

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Database Provider: InMemory (default, no persistence) or PostgreSQL (persistent)
DATABASE_PROVIDER=InMemory

# PostgreSQL Connection (only needed if DATABASE_PROVIDER=PostgreSQL)
# Uncomment and configure these if using PostgreSQL:
# POSTGRES_PASSWORD=your-secure-password
# POSTGRES_CONNECTION_STRING=Host=postgres;Database=aimate;Username=aimate;Password=your-secure-password

# ==============================================================================
# SECURITY
# ==============================================================================

# JWT Secret Key for authentication (REQUIRED - change in production!)
JWT_SECRET=your-secret-key-change-in-production

# ==============================================================================
# AI PROVIDERS
# ==============================================================================

# LiteLLM Master Key (for accessing the LiteLLM gateway)
LITELLM_MASTER_KEY=sk-1234

# OpenAI API Key (optional - if using OpenAI directly)
OPENAI_API_KEY=

# Anthropic API Key (optional - if using Claude)
ANTHROPIC_API_KEY=

# Google API Key (optional - if using Gemini)
GOOGLE_API_KEY=

# ==============================================================================
# DEPLOYMENT NOTES
# ==============================================================================

# Default Configuration (InMemory Database):
# - Quick setup for testing
# - No database persistence (data lost on restart)
# - Minimal resource requirements
# - Run with: docker-compose -f docker-compose.production.yml up

# PostgreSQL Configuration:
# - Full database persistence
# - Requires running PostgreSQL service
# - Set DATABASE_PROVIDER=PostgreSQL
# - Configure POSTGRES_PASSWORD and POSTGRES_CONNECTION_STRING
# - Run with: docker-compose -f docker-compose.production.yml --profile with-postgres up

# With Nginx Reverse Proxy:
# - Adds nginx for SSL termination and load balancing
# - Run with: docker-compose -f docker-compose.production.yml --profile with-nginx up

# Full stack with all services:
# - Run with: docker-compose -f docker-compose.production.yml --profile with-postgres --profile with-nginx up
